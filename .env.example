# Server-only OpenF1 access token for live positions, gaps, laps and race control.
# Create/manage access at https://openf1.org/auth.html and configure the same
# variable in the Vercel Production environment.
OPENF1_API_TOKEN=
